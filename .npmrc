# npm 12 refuses git dependencies by default. The shared core is installed straight from its
# pinned commit rather than the registry, so it is allowed here - and only at the root, so no
# transitive dependency can pull in a git URL of its own.
allow-git=root
